<%= form_for @items do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control', placeholder: "Enter to-do name" %>
  </div>
  <%= f.submit "Item saved" %>
<% end %>
